{
  "scripts": {
    "build": "cd src && npm ci && tsc",
    "test": "jest ./src/lib",
    "deploy-local": "cd src && tsc && cpx package.json dist && cd dist && yalc publish --push"
  },
  "devDependencies": {
    "@types/jest": "27.4.1",
    "jest": "27.5.1",
    "jest-config": "28.1.0",
    "typescript": "4.6.2",
    "ts-jest": "29.0.3",
    "ts-node": "10.9.1"
  }
}
