{
  "scripts": {
    "test": "jest ./src/lib",
    "deploy-local": "cd src && tsc && cpx package.json dist && cpx ../README.md dist && cd dist && yalc publish --push"
  },
  "author": "navez.jerome@gmail.com",
  "devDependencies": {
    "@types/jest": "27.4.1",
    "jest": "27.5.1",
    "jest-config": "28.1.0",
    "typescript": "4.6.2",
    "ts-jest": "29.0.3",
    "ts-node": "10.9.1"
  }
}
